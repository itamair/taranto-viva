services:
  logger.channel.custom_module:
    parent: logger.channel_base
    arguments: ['custom_module']

  custom_module.overture_utility:
    class: Drupal\custom_module\OverturemapsUtility
    arguments:
      - '@http_client'
      - '@cache.discovery'
      - '@logger.channel.custom_module'
